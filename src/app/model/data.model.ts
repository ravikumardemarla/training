export interface DataResponse {
    userId: string;
    id: string;
    title: string;
}


export interface User {
    first: string;
    last: string;
}

export interface EmailInfo {
    email: string;
    routing: string;
}
